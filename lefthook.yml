pre-commit:
  parallel: true
  commands:
    lint:
      run: npx pnpm biome check --staged
    typecheck:
      run: npx pnpm typecheck
    validate-quotes:
      glob: "packages/data/src/quotes.json"
      run: npx pnpm --filter @qquotes/data validate
